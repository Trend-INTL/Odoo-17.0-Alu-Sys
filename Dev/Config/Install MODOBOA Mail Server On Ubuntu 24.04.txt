1- Add the Following Records to your Domain Name Server DNS

To ADD SUBDOMAINS:
	Type	Name			Priority			Content/IP			TTL
	MX	@			10				mail.trend-ai.net		200	No Need
	A	mail			0				195.35.28.250			200	No Need
---------------------------------------------------------------------------------------------------------------------
import distutils.cmd

/sudo apt install software-properties-common
/sudo add-apt-repository ppa:deadsnakes/ppa
# sudo nano /usr/bin/add-apt-repository
/sudo apt install python3.8
sudo wget https://www.python.org/ftp/python/3.8.2/Python-3.8.2rc2.tgz
cd Python-3.8.2rc2
sudo ./configure --enable-optimizations
sudo make install


# sudo apt list | grep python3.8
# sudo apt-get install python3.8
# sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.8 2
# sudo update-alternatives --config python3
# python3 --version
# sudo apt remove python3-pip
sudo apt install python3-pip
# sudo apt install python3-pip --reinstall
# pip install --upgrade pip 
pip --version
# sudo cp apt_pkg.cpython-38-x86_64-linux-gnu.so apt_pkg.so
# sudo nano /usr/bin/add-apt-repository
# Change the first line to ==>>> #!/usr/bin/python3.8


sudo apt update
sudo apt upgrade

cd /etc/nginx/sites-enabled
sudo nano default
/sudo nano nginx.conf

/sudo mkdir modoboa
/sudo chown trend:trend modoboa
/sudo chmod +x modoboa
/ls -la
/cd modoboa

/  $ git clone https://github.com/modoboa/modoboa.git --branch 1.8.x --depth 1
/  $ git clone https://github.com/modoboa/modoboa-installer  --branch 17.0 --depth 1
/  $ cd modoboa-installer
/  $ sudo python3 run.py trend-ai.net
/  $ git clone https://github.com/Mailu/Mailu.git
/ sudo apt-get install python3-virtualenv python3-pip git curl gnupg2

/sudo apt get install -y git python3-vertualenv python3-pip
git clone https://github.com/modoboa/modoboa-installer.git	==>>> Download mondoboa mail server from github
cd modoboa-installer

/python3.8 -m venv modoboa_env
/source modoboa_env/bin/activate

/sudo ./run.py --stop-after-configfile-check mail.trend-ai.net =>> Create a Configuration file for mondoboa
sudo ./run.py --stop-after-configfile-check trend-ai.net =>> Create a Configuration file for mondoboa
sudo chown trend:trend installer.cfg
sudo chmod +x installer.cfg
sudo nano installer.cfg		=>> Change Configurations

-------------------------------------------------------------------------
"
[general]
hostname = mail.%(domain)s

[certificate]
type = letsencrypt
tls_cert_file_path =
tls_key_file_path =

[letsencrypt]
email = admin@trend-ai.net

[database]
engine = postgres
host = 127.0.0.1
install = true

[postgres]
user = postgres =>>> trend
password = 	=>>> trend123
"
-----------------------------------------------------------------------------

/sudo nano /etc/nginx/sites-enabled/default
=========================================
server {
        listen 443 default_server;
        listen [::]:443 default_server;
        listen 80 default_server;
        listen [::]:80 default_server;
==========================================
/sudo service nginx reload

/sudo nano README.rst

/python3.10 -m venv modoboa_env
/source modoboa_env/bin/activate

/cd /srv
/sudo mkdir /srv/modoboa
/sudo chown trend:trend /srv/modoboa
/sudo chmod +X /srv/modoboa
-------------------------------------
/python3 -m venv /srv/modoboa/env
/source /srv/modoboa/env/bin/activate
/cd /srv/modoboa/env/Scripts/activate
/python -m ensurepip
/pip --version
/pip install --upgrade pip
pip install --upgrade pip setuptools
pip install --no-binary :all: django-xforwardedfor-middleware progressbar33 rrdtool sgmllib3k


/sudo -H pip install modoboa


sudo ./run.py trend-ai.net

sudo ./run.py --interactive trend-ai.net
IF ERROR : sudo ./run.py --interactive trend-ai.net --debug
run sudo --configure -a
sudo ./run.py --version=1.8.1 trend-ai.net
sudo ./run.py --version=1.8.1 trend-ai.net --debug
pip install --upgrade modoboa

# Deactivate the virtual environment
deactivate

sudo nano /etc/nginx/sites-enabled/mail.conf


=====================================================================================================================================
Manual Installation:

If all else fails, you might need to manually download the source code of the problematic packages and attempt to build and install them manually. This gives you more control over the installation process and allows you to debug any issues that arise.
If you continue to encounter problems after trying these steps, it might be helpful to provide more details about your environment (such as Python version, operating system, etc.) and the specific commands you're running so that I can offer more targeted assistance.

sudo -u trend -i bash
python3.10 -m venv modoboa_env
source modoboa_env/bin/activate
pip install --upgrade pip
pip install modoboa==1.8.0
cd modoboa
> sudo python3 manage.py migrate
> python3 manage.py collectstatic
> python3 manage.py check --deploy
